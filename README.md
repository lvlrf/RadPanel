# 🚀 RAD Panel - VPN Sales & Agency Management

**نسخه**: 1.0.0 MVP  
**وضعیت**: ✅ آماده استقرار  
**زبان**: فارسی (RTL Support)

---

## 📋 خلاصه

**RAD Panel** یک سیستم جامع مدیریت فروش و نمایندگی VPN است که به عنوان لایه مدیریتی روی پنل مرزبان قرار می‌گیرد.

### ویژگی‌های کلیدی
- ✅ سیستم نقش‌ها (مدیر، نماینده، کاربر نهایی)
- ✅ **اعتبار امانی** برای پرداخت‌های در انتظار تایید
- ✅ اتصال به مرزبان با پشتیبانی پروکسی
- ✅ مدیریت کامل پلن‌ها و قیمت‌گذاری
- ✅ سیستم مالی کامل با Ledger
- ✅ گزارش‌گیری و خروجی Excel
- ✅ UI/UX مدرن و responsive
- ✅ حالت شب/روز
- ✅ امنیت کامل (JWT, bcrypt, HTTPS)

---

## 🏗️ معماری

```
┌─────────────────────────────────────────┐
│         Internet (HTTPS)                │
└──────────────┬──────────────────────────┘
               │
        ┌──────▼──────┐
        │   Nginx     │  SSL/Reverse Proxy
        │  (Port 443) │
        └──────┬──────┘
               │
    ┌──────────┼──────────┐
    │                     │
┌───▼────┐          ┌────▼─────┐
│ React  │          │ FastAPI  │
│Frontend│          │ Backend  │
│ (SPA)  │          │(Port 8000)│
└────────┘          └────┬─────┘
                         │
               ┌─────────┼──────────┐
               │                    │
        ┌──────▼─────┐      ┌──────▼───────┐
        │PostgreSQL  │      │  Marzban API │
        │    15      │      │ (via Proxy)  │
        └────────────┘      └──────────────┘
```

---

## 📁 ساختار پروژه

```
rad-panel/
├── backend/                 # FastAPI Backend
│   ├── app/
│   │   ├── main.py         # FastAPI app
│   │   ├── config.py       # Settings
│   │   ├── database.py     # DB connection
│   │   ├── models/         # SQLAlchemy models
│   │   ├── schemas/        # Pydantic schemas
│   │   ├── api/            # API endpoints
│   │   ├── services/       # Business logic
│   │   ├── utils/          # Helpers
│   │   └── jobs/           # Background jobs
│   ├── alembic/            # Migrations
│   ├── requirements.txt
│   └── Dockerfile
│
├── frontend/               # React Frontend
│   ├── src/
│   │   ├── components/    # UI components
│   │   ├── pages/         # Route pages
│   │   ├── services/      # API clients
│   │   ├── hooks/         # Custom hooks
│   │   ├── utils/         # Helpers
│   │   ├── App.jsx
│   │   └── main.jsx
│   ├── package.json
│   └── Dockerfile
│
├── docker/                 # Docker configs
│   ├── docker-compose.yml
│   ├── docker-compose.prod.yml
│   └── nginx/
│       └── nginx.conf
│
├── docs/                   # Documentation
│   ├── 00_context/        # PRD, decisions
│   ├── 10_product/        # Tasks, specs
│   ├── 20_engineering/    # Architecture, schema
│   ├── 30_design/         # UI/UX
│   ├── 40_api/            # API docs
│   └── 90_ops/            # Deployment
│
├── .env.example           # Environment template
├── README.md              # This file
└── QUICK_START.md         # 5-minute setup
```

---

## ⚡ نصب سریع (5 دقیقه)

### پیش‌نیازها
- Docker & Docker Compose
- یک دامنه با SSL
- اطلاعات پنل مرزبان

### مراحل

```bash
# 1. کپی کردن پروژه
cd /var
unzip rad-panel.zip
cd rad-panel

# 2. تنظیم environment
cp .env.example .env
nano .env  # ویرایش کنید

# 3. راه‌اندازی
docker compose up -d

# 4. بررسی وضعیت
docker compose ps
docker compose logs -f

# 5. دسترسی به پنل
# https://your-domain.com
# Username: admin
# Password: admin123
```

**⚠️ مهم**: بلافاصله رمز عبور admin را تغییر دهید!

---

## 🔧 تنظیمات

### فایل .env

```bash
# Database
POSTGRES_USER=radpanel
POSTGRES_PASSWORD=your_secure_password
POSTGRES_DB=radpanel
DATABASE_URL=postgresql://radpanel:password@db:5432/radpanel

# Security
JWT_SECRET=your_64_character_random_secret  # openssl rand -hex 32
BCRYPT_ROUNDS=12

# Marzban
MARZBAN_URL=https://your-marzban.com
MARZBAN_USERNAME=admin
MARZBAN_PASSWORD=your_marzban_password

# پروکسی (اختیاری - اگر پنل در ایران و مرزبان در خارج)
HTTP_PROXY=http://proxy:port
HTTPS_PROXY=http://proxy:port

# Panel
PANEL_NAME=RAD Panel
PANEL_DOMAIN=panel.example.com
```

---

## 👥 نقش‌های کاربری

### 1️⃣ مدیر (Admin)
- مدیریت نمایندگان (ایجاد، ویرایش، غیرفعال)
- مدیریت پلن‌ها (CRUD)
- مدیریت روش‌های پرداخت
- تایید/رد پرداخت‌ها
- گزارش‌گیری و Excel export
- افزایش/کاهش دستی اعتبار

**دسترسی پیش‌فرض**:
- Username: `admin`
- Password: `admin123`

### 2️⃣ نماینده (Agent)
- مشاهده کیف پول (اعتبار تایید شده + امانی)
- آپلود رسید برای شارژ
- ایجاد کاربر VPN
- مدیریت کاربران ساخته شده
- دریافت لینک subscription

### 3️⃣ کاربر نهایی (End-user)
- ثبت‌نام عمومی
- خرید اکانت VPN
- آپلود رسید پرداخت
- مشاهده اکانت‌های خریداری شده

---

## 💰 سیستم اعتبار امانی

### فلسفه
به جای اینکه کاربران منتظر تایید پرداخت بمانند، بلافاصله **اعتبار امانی** دریافت می‌کنند و می‌توانند کار کنند. مدیر ظرف 24 ساعت بررسی و تایید/رد می‌کند.

### فرآیند

```
1. آپلود رسید
   ↓
2. اعتبار امانی اضافه می‌شود (فوری)
   ↓
3. کاربر می‌تواند خرج کند
   ↓
4. مدیر بررسی می‌کند (ظرف 24h)
   ↓
5a. تایید → امانی تبدیل به قطعی
5b. رد → اعتبار منفی
   ↓ (اگر منفی)
6. قفل ساخت کاربر 24h
   ↓
7. بعد 24h → غیرفعال همه کاربران
```

### مثال

```
نماینده 1,000,000 تومان رسید آپلود می‌کند
→ اعتبار امانی: +1,000,000
→ می‌تواند خرج کند

نماینده 1,500,000 خرج می‌کند (داشت 500K تایید شده قبلی)
→ تایید شده: 0
→ امانی: 1,000,000
→ خرج شده: 1,500,000
→ باقیمانده: 500,000

مدیر رد می‌کند
→ امانی: 0
→ تایید شده: 0
→ باقیمانده: -500,000 (منفی!)

→ نمی‌تواند کاربر بسازد 24h
→ بعد 24h همه کاربرانش غیرفعال
```

---

## 🎯 قوانین 24 ساعته

### قانون 1: قفل ساخت
```
اگر اعتبار < 0:
  → 24 ساعت نمی‌تواند کاربر بسازد
  → پیام: "اعتبار منفی - لطفاً شارژ کنید"
```

### قانون 2: غیرفعال خودکار
```
اگر بعد 24 ساعت هنوز منفی:
  → همه کاربران غیرفعال
  → پیام هشدار قبلی ارسال شده
```

### قانون 3: بازگشت اعتبار
```
اگر کاربر حذف شد < 24h:
  → اعتبار بر اساس روز و گیگ باقیمانده برمی‌گردد
```

---

## 📊 پلن‌های VPN

مدیر می‌تواند از داشبورد پلن‌ها را مدیریت کند:

### ساختار پلن
- **نام**: مثلاً "پلن استاندارد"
- **روز**: 30
- **گیگ**: 50
- **قیمت عمومی**: 80,000 تومان
- **قیمت نماینده**: 65,000 تومان
- **وضعیت**: فعال/غیرفعال

### پلن‌های پیشنهادی
1. **پایه**: 30GB / 30 روز - 50K / 40K
2. **استاندارد**: 50GB / 30 روز - 80K / 65K
3. **پریمیوم**: 100GB / 30 روز - 140K / 110K

---

## 💳 روش‌های پرداخت

### 1. کارت به کارت
- شماره کارت
- نام صاحب حساب
- نام بانک
- **چرخش تصادفی**: هر بار یک کارت رندم نمایش داده می‌شود

### 2. شبا
- شماره شبا
- نام صاحب حساب

### 3. ارز دیجیتال
- نوع کوین (USDT TRC20, BTC, ...)
- آدرس کیف پول
- **بونوس**: 10% اعتبار بیشتر برای پرداخت کریپتو

---

## 📈 گزارشات

### خروجی Excel شامل:
- تاریخ و ساعت
- نماینده
- نوع تراکنش (شارژ/سفارش/برگشت)
- مبلغ
- وضعیت (تایید/رد/در انتظار)
- روش پرداخت
- توضیحات

### فیلترها:
- بازه زمانی
- نماینده خاص
- وضعیت پرداخت
- نوع تراکنش

---

## 🔐 امنیت

### احراز هویت
- JWT tokens (httpOnly cookies)
- bcrypt password hashing (cost: 12)
- Session expiry: 7 days

### دسترسی
- Role-based access control
- نمایندگان فقط داده‌های خودشان
- Middleware برای بررسی نقش

### محافظت
- Rate limiting (Login: 5/15min, API: 100/min)
- HTTPS اجباری
- SQL injection prevention
- XSS protection
- CORS configuration

### بکاپ
- خودکار روزانه (3 AM)
- نگهداری 30 روز
- فشرده‌سازی (gzip)

---

## 🚀 عملیات

### دستورات مفید

```bash
# مشاهده وضعیت
docker compose ps

# Logs
docker compose logs -f
docker compose logs -f backend

# راه‌اندازی مجدد
docker compose restart
docker compose restart backend

# توقف
docker compose down

# Build مجدد
docker compose up -d --build

# ورود به container
docker compose exec backend bash
docker compose exec db psql -U radpanel

# بکاپ دستی
docker compose exec db pg_dump -U radpanel radpanel > backup.sql

# Restore
cat backup.sql | docker compose exec -T db psql -U radpanel radpanel
```

### Monitoring

```bash
# Resource usage
docker stats

# Database queries
docker compose exec db psql -U radpanel -d radpanel -c "
SELECT query, calls, total_time, mean_time
FROM pg_stat_statements
WHERE mean_time > 100
ORDER BY total_time DESC
LIMIT 10;"

# Slow queries
docker compose logs backend | grep "slow"
```

---

## 🔄 به‌روزرسانی

```bash
# دانلود نسخه جدید
cd /var/rad-panel
unzip -o rad-panel-v2.zip

# Build
docker compose build

# راه‌اندازی
docker compose up -d

# Migrations
docker compose exec backend alembic upgrade head

# بررسی
docker compose logs -f
```

---

## 🐛 عیب‌یابی

### مشکل: Container راه‌اندازی نمی‌شود
```bash
docker compose logs
docker compose down && docker compose up -d
```

### مشکل: دیتابیس connect نمی‌شود
```bash
docker compose exec db pg_isready -U radpanel
docker compose restart db
```

### مشکل: اتصال به مرزبان
```bash
# بررسی proxy
docker compose logs backend | grep "Marzban"

# تست دستی
docker compose exec backend python -c "
import httpx
client = httpx.Client(proxies='http://your-proxy:port')
r = client.get('https://your-marzban.com')
print(r.status_code)
"
```

### مشکل: SSL
```bash
sudo certbot certificates
sudo certbot renew
docker compose restart nginx
```

---

## 📚 مستندات

- **کاربری**: [USER_MANUAL.md](docs/USER_MANUAL.md)
- **فنی**: [ARCHITECTURE.md](docs/20_engineering/ARCHITECTURE.md)
- **دیتابیس**: [DATABASE_SCHEMA.md](docs/20_engineering/DATABASE_SCHEMA.md)
- **API**: https://your-domain.com/docs (Swagger)
- **استقرار**: [DEPLOYMENT_GUIDE.md](docs/90_ops/DEPLOYMENT_GUIDE.md)

---

## 🗺️ نقشه راه (Roadmap)

### ✅ فاز 1 - MVP (فعلی)
- سیستم نقش‌ها
- اعتبار امانی
- مدیریت پلن‌ها
- اتصال به مرزبان
- گزارشات Excel

### 🔜 فاز 2 (بعدی)
- تلگرام بات
- OTP برای کاربران
- اطلاع‌رسانی‌ها
- Referral system

### 🔮 فاز 3 (آینده)
- زیرنماینده
- White-label
- سیستم سکه/کوین
- Multi-panel support

---

## 🤝 پشتیبانی

### مشکل یافتید؟
1. بررسی [TROUBLESHOOTING.md](docs/TROUBLESHOOTING.md)
2. مشاهده logs: `docker compose logs -f`
3. بررسی [GitHub Issues](https://github.com/your-repo/rad-panel/issues)

### درخواست ویژگی جدید؟
- ایجاد issue در GitHub
- توضیح دقیق use case
- ارائه مثال

---

## 📄 مجوز

این پروژه تحت مجوز [MIT License](LICENSE) منتشر شده است.

---

## 🙏 تشکر

ساخته شده با ❤️ برای جامعه VPN ایران

**Technologies**:
- FastAPI
- React
- PostgreSQL
- Docker
- Nginx
- Marzban

---

**نسخه**: 1.0.0 MVP  
**تاریخ**: 2025-01-01  
**وضعیت**: ✅ Production Ready

---

## 🚦 شروع سریع

```bash
# 1 دقیقه تا راه‌اندازی!
cd /var && unzip rad-panel.zip && cd rad-panel
cp .env.example .env && nano .env
docker compose up -d
```

**دسترسی**: https://your-domain.com  
**Admin**: admin / admin123

**موفق باشید! 🎉**
