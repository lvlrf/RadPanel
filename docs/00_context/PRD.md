# مستندات محصول - پنل فروش RAD

**نسخه**: 1.0.0 (MVP)  
**تاریخ**: 2025-01-01  
**وضعیت**: در حال توسعه

---

## خلاصه اجرایی

**RAD Panel** یک سیستم مدیریت فروش و نمایندگی VPN است که به عنوان لایه مدیریتی روی پنل مرزبان قرار می‌گیرد. این پنل امکان مدیریت نمایندگان، فروش خودکار، مدیریت مالی و تحویل سرویس را فراهم می‌کند.

### هدف اصلی
حل مشکل مدیریت دستی نمایندگان و کاهش فشار کاری مدیر از طریق اتوماسیون فرآیند فروش و تحویل.

### کاربران هدف
1. **مدیر سیستم** - مالک بیزینس VPN
2. **نماینده فروش** - افرادی که سرویس VPN می‌فروشند
3. **کاربر نهایی** - مشتریان نهایی که VPN می‌خرند

---

## ویژگی‌های کلیدی (MVP)

### ● هسته سیستم
- ✅ سیستم نقش‌ها: مدیر / نماینده / کاربر نهایی
- ✅ احراز هویت با نام‌کاربری و رمز عبور
- ✅ مدیریت کیف پول و اعتبار برای نمایندگان
- ✅ سیستم "اعتبار امانی" برای پرداخت‌های در انتظار تایید

### ● برای مدیر
- ✅ ساخت و مدیریت نمایندگان
- ✅ تعریف پلن‌های VPN (روز/گیگ/قیمت)
- ✅ مدیریت روش‌های پرداخت (کارت/شبا/کریپتو)
- ✅ تایید/رد پرداخت‌ها با امکان ثبت توضیحات
- ✅ افزایش/کاهش دستی اعتبار
- ✅ گزارش‌گیری و خروجی اکسل
- ✅ چرخش تصادفی شماره کارت‌ها

### ● برای نماینده
- ✅ مشاهده اعتبار (تایید شده + امانی)
- ✅ ثبت سفارش و ساخت کاربر
- ✅ آپلود رسید پرداخت برای شارژ کیف پول
- ✅ مدیریت کاربران ساخته شده
- ✅ دریافت لینک اشتراک (Subscription)

### ● برای کاربر نهایی
- ✅ ثبت‌نام عمومی
- ✅ خرید اکانت VPN
- ✅ آپلود رسید پرداخت
- ✅ دریافت لینک اشتراک

### ● اتصال به مرزبان
- ✅ ساخت خودکار کاربر در مرزبان
- ✅ مدیریت اشتراک‌ها
- ✅ بررسی یکتا بودن نام‌کاربری
- ✅ پشتیبانی از پروکسی برای دسترسی به API

---

## منطق اعتبار و پرداخت

### سیستم اعتبار امانی

**فلسفه**: به جای اینکه نماینده منتظر تایید پرداخت بماند، بلافاصله اعتبار "امانی" دریافت می‌کند تا بتواند کار کند. مدیر ظرف 24 ساعت پرداخت را بررسی و تایید/رد می‌کند.

#### فرآیند:
1. **آپلود رسید**: نماینده/کاربر رسید پرداخت را آپلود می‌کند
2. **اعتبار امانی**: سیستم فوراً کل مبلغ را به عنوان "اعتبار امانی" اضافه می‌کند
3. **امکان استفاده**: نماینده می‌تواند تا سقف (اعتبار تایید شده + امانی) خرج کند
4. **بررسی مدیر**: ظرف 24 ساعت مدیر بررسی می‌کند
5. **تایید**: اعتبار امانی → اعتبار تایید شده
6. **رد**: اعتبار منفی می‌شود

### محدودیت‌ها
- ✅ نماینده/کاربر نمی‌تواند بیشتر از مجموع اعتبارش خرج کند
- ✅ پرداخت کریپتو: تایید دستی
- ✅ قیمت پلن‌ها همیشه قیمت فعلی است (نه قدیمی)

---

## قوانین 24 ساعته

### برای اعتبار منفی:
1. **0-24 ساعت اول**: نماینده نمی‌تواند کاربر جدید بسازد
   - پیام: "اعتبار منفی - لطفاً شارژ کنید"
2. **بعد از 24 ساعت**: اگر شارژ نکرد
   - همه کاربران او غیرفعال می‌شوند
   - پیام هشدار قبلی ارسال می‌شود

### برای حذف/غیرفعال کاربر:
- **حذف کاربر < 24 ساعت**: اعتبار بر اساس روز باقیمانده و گیگ مصرف نشده برمی‌گردد
- **غیرفعال کردن**: اعتبار برمی‌گردد (چون در حال استفاده نیست)

---

## روش‌های پرداخت

### 1. کارت به کارت
**فیلدها**:
- اسم مستعار (برای نمایش)
- شماره کارت
- نام صاحب حساب
- نام بانک
- وضعیت (فعال/غیرفعال)
- محدودیت تعداد روزانه (اختیاری)
- محدودیت مبلغ روزانه (اختیاری)
- توضیحات

**ویژگی خاص**: چرخش تصادفی
- هر بار یک کارت رندم از لیست فعال‌ها نمایش داده می‌شود

### 2. شبا
مشابه کارت به کارت ولی با شماره شبا

### 3. ارز دیجیتال
**فیلدها**:
- اسم مستعار
- نوع کوین (USDT TRC20, Bitcoin, etc.)
- آدرس کیف پول
- شبکه
- وضعیت (فعال/غیرفعال)
- درصد اعتبار اضافه (پیش‌فرض: 10%)
- محدودیت‌ها
- توضیحات

**مثال**: پرداخت با کریپتو → 10% اعتبار بیشتر

---

## پلن‌های VPN

مدیر می‌تواند پلن‌ها را از داشبورد مدیریت کند (CRUD کامل).

### ساختار پلن:
```
- نام پلن: مثلاً "پلن استاندارد"
- تعداد روز: 30
- حجم (گیگ): 50
- قیمت عمومی: 80,000 تومان (برای کاربر نهایی)
- قیمت نماینده: 65,000 تومان (برای نماینده)
- وضعیت: فعال/غیرفعال
```

### مثال پلن‌ها:
1. **پایه**: 30GB / 30 روز - عمومی: 50K - نماینده: 40K
2. **استاندارد**: 50GB / 30 روز - عمومی: 80K - نماینده: 65K
3. **پریمیوم**: 100GB / 30 روز - عمومی: 140K - نماینده: 110K

---

## ساخت کاربر در مرزبان

### فیلدهای ورودی (توسط نماینده/کاربر):
- **اسم مستعار**: نام نمایشی (اختیاری)
- **نام‌کاربری**: یکتا (چک می‌شود)
- **روز**: تعداد روز اعتبار
- **گیگ**: حجم مصرف
- **On Hold**: فعال/غیرفعال (تنظیمات مرزبان)

### اطلاعات ذخیره شده در Note مرزبان:
```
RAD Panel User
Created by: [نام نماینده]
Created at: [تاریخ و ساعت]
Plan: [نام پلن]
Price: [قیمت]
Agent Note: [توضیحات نماینده]
Last Modified: [تاریخ آخرین تغییر]
```

### Inbound ها:
- همه inbound های فعال مرزبان (یا پیش‌فرض پنل)

---

## گزارشات و اکسل

### ستون‌های گزارش اکسل:
1. تاریخ
2. ساعت
3. نماینده (نام)
4. نوع تراکنش (شارژ/سفارش/برگشت)
5. مبلغ
6. وضعیت (تایید/رد/در انتظار)
7. روش پرداخت
8. توضیحات

### فیلترها:
- بازه زمانی
- نماینده خاص
- وضعیت پرداخت
- نوع تراکنش

---

## امنیت

### احراز هویت:
- Username + Password
- JWT Token
- bcrypt برای هش کردن رمز عبور

### دسترسی‌ها:
- **مدیر**: دسترسی کامل
- **نماینده**: فقط به کاربران و سفارشات خودش
- **کاربر نهایی**: فقط به اکانت‌های خودش

### محافظت:
- Rate limiting
- HTTPS اجباری
- .env برای اطلاعات حساس

---

## اتصال به مرزبان

### اطلاعات مورد نیاز (.env):
```
MARZBAN_URL=https://your-marzban-panel.com
MARZBAN_USERNAME=admin
MARZBAN_PASSWORD=your_password
```

### پشتیبانی از پروکسی:
```
HTTP_PROXY=http://proxy-server:port
HTTPS_PROXY=http://proxy-server:port
```

این امکان را می‌دهد که پنل از ایران به مرزبان خارج متصل شود.

---

## استقرار (Deployment)

### پلتفرم:
- سرور ایران
- Ubuntu 22.04 / 24.04
- Docker + Docker Compose

### دامنه:
- HTTPS اجباری
- Let's Encrypt SSL
- Nginx Reverse Proxy

### بکاپ:
- بکاپ خودکار دیتابیس روزانه

---

## فاز بعدی (خارج از MVP)

این‌ها در نسخه اول نیستند:

### ○ تلگرام بات
- OTP برای کاربر نهایی
- Share contact برای احراز شماره
- اطلاع‌رسانی‌ها
- دسترسی به امکانات پنل

### ○ زیرنماینده و Referral
- نمایندگان بتوانند زیرنماینده بگیرند
- سیستم کمیسیون چندسطحی

### ○ White-label
- نمایندگان دامنه جداگانه
- برندینگ سفارشی
- تنظیمات مالی مستقل

### ○ سیستم سکه/کوین
- واحد پولی داخلی
- جلوگیری از ضرر در تغییر قیمت

### ○ چرخش هوشمند حساب‌ها
- محدودیت استفاده روزانه
- تغییر عدد انتهای مبلغ

### ○ Admin Impersonation
- ورود به پنل نماینده از طرف مدیر

### ○ اتصال به پنل‌های دیگر
- Sanaei
- Hiddify
- OpenVPN
- MikroTik

---

## 🚫 خارج از محدوده (Out of Scope)

این‌ها اصلاً ساخته نمی‌شوند:

- درگاه پرداخت آنلاین (فعلاً فقط رسید دستی)
- اپلیکیشن موبایل Native
- پنل مدیریتی جداگانه (Web-based admin)
- سیستم تیکتینگ پیچیده (فعلاً تلگرام)
- Multi-language (فقط فارسی فعلاً)

---

## نام پروژه و برندینگ

**نام**: RAD Panel  
**لوگو**: فعلاً emoji (🚀)  
**طراحی**: مینیمال، تمرکز بر فروش و کارایی  
**رنگ‌بندی**: حالت روز/شب

---

## تماس و پشتیبانی

در صورت سوال یا مشکل:
- مستندات فنی: `/docs/20_engineering/`
- راهنمای استقرار: `/docs/90_ops/DEPLOYMENT_GUIDE.md`

---

**نسخه**: 1.0.0 MVP  
**آخرین بروزرسانی**: 2025-01-01  
**وضعیت**: ✅ تایید شده برای توسعه
